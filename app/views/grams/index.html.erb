<!-- firehose track 5, lesson 5 - file created for writing first test -->

<h1>Grams!</h1>

<!-- code block added 12 jul 17 for uploading images (lesson/challenge 16), deleted/modified (new block below) for styling (lesson 19)
<% @grams.each do |gram| %>
<div>
  <%= gram.message %>
  <%= image_tag gram.picture %>
</div>
<% end %>  -->

<div class="col-10 offset-1 col-sm-8 offset-sm-2 col-md-6 offset-md-3">
  <% @grams.each do |gram| %>
  <div class="card">
    <div class="handle"><%= gram.user.try(:email) %></div>
    <%= image_tag gram.picture, class: 'img-fluid' %>
    <p>
      <%= gram.message %>
    </p>
  </div>
  <% end %>
</div>
